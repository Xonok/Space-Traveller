This is the endpoint for code migration. Any new code should be placed according to this system:
**lib** - generic libraries that are not intended to change. Among those is *func*, which is the container for any code too small to have its own library (yet).

